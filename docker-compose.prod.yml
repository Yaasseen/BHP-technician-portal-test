version: '3.8'

services:
  # PHP Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: technician-portal-app
    container_name: technician-portal-app
    restart: always
    working_dir: /var/www
    env_file:
      - .env
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=${APP_URL:-https://bph-technical.durgagupta.com.np}
      - TZ=Indian/Mauritius
    volumes:
      # Only persist storage, do NOT mount code (use image code)
      - ./storage:/var/www/storage
      # Share public assets with web container
      - static-content:/var/www/public
    networks:
      - technician-network
    depends_on:
      - db

  # Scheduler
  scheduler:
    build:
      context: .
      dockerfile: Dockerfile
    image: technician-portal-app
    container_name: technician-portal-scheduler
    restart: always
    working_dir: /var/www
    command: php artisan schedule:work
    env_file:
      - .env
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=${APP_URL:-https://bph-technical.durgagupta.com.np}
      - TZ=Indian/Mauritius
    volumes:
      - ./storage:/var/www/storage
    networks:
      - technician-network
    depends_on:
      - app
      - db

  # Nginx Web Server
  web:
    image: nginx:alpine
    container_name: technician-portal-nginx
    restart: always
    environment:
      - APP_DOMAIN=${APP_DOMAIN:-bph-technical.durgagupta.com.np}
      - TZ=Indian/Mauritius
    ports:
      - "6002:80"
    volumes:
      - ./nginx/conf.d/:/etc/nginx/templates/
      # Get public assets from the shared volume populated by 'app'
      - static-content:/var/www/public
    networks:
      - technician-network
    depends_on:
      - app

  # Database
  db:
    image: mysql:8.0
    ports:
      - "33060:3306"
    container_name: technician-portal-db
    restart: always
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-tbh_portal_prod}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      TZ: Indian/Mauritius
    volumes:
      - ./mysql-data:/var/lib/mysql
    networks:
      - technician-network

networks:
  technician-network:
    driver: bridge

volumes:
  static-content:
